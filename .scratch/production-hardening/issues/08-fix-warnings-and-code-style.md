# 08 — Fix build warnings and apply IDE code-style suggestions

**What to build:** A whole-codebase mechanical cleanup pass, run once the structural and feature work (splits, new processors' capability query, both cache backends, both new test suites) has landed, so it sweeps the finished state instead of being redone after each subsequent ticket. Eliminate all compiler warnings across every project in the solution, and apply current-generation IDE code-style suggestions consistently (e.g. primary constructors where applicable, collection expressions, other analyzer-suggested modernizations) — mechanical, behavior-preserving changes only, no functional edits. This is a **wide refactor** in the sense that its blast radius spans the whole codebase, but it's low-risk (style/warnings only) so it lands as a single ticket rather than expand-contract batches.

**Blocked by:** 01, 03, 04, 05, 06, 07 (sweeps the code those tickets add/move, so it runs once, last, before the codebase's warning/style baseline is locked in by CI).

**Status:** resolved

- [x] Solution builds with zero compiler warnings across all projects (Core, UmbracoExtensions, SkiaSharp, ImageFlow, Service, Umbraco sample, AppHost, and all test projects)
- [x] Current IDE/analyzer style suggestions are applied consistently (primary constructors where applicable, and other flagged modernizations)
- [x] No behavior change — full test suite (existing + parity + cache-contract + integration) still passes after the sweep
- [x] `Directory.Build.props` (or equivalent) is updated so warnings are treated as errors going forward, preventing regression

## Comments

- **Baseline going in**: `dotnet build -t:Rebuild` was already at 0 compiler warnings before this ticket (nothing here was a real compile-time warning). `dotnet format --verify-no-changes --severity info` was the actual signal — it surfaced 31 IDE/analyzer/xUnit suggestions across 17 files that were real but not yet enforced at build time.
- **Auto-applied via `dotnet format --severity info`**: 12x `IDE0290` (use primary constructor), 1x `IDE0305` (collection-expression simplification, in the new ticket-06 `CrossProcessorParityTests.cs`), and, unexpectedly, all 4x `CA1822` (`SignedResizeUrl`/`SignedOriginUrl` in `UmbracoServiceGraphHarness.cs`, `Unsigned`/`Tampered` in `SignedRequestUrlBuilder.cs` — `dotnet format` fixed these members to `static` itself and rewrote their call sites, including in `E2E.Tests`). One caveat: running `dotnet format` unrestricted crashed with `NotSupportedException: Changing document properties is not supported` while attempting the `IDE0130` namespace-move fix, so the sweep was run with `--exclude-diagnostics IDE0130` and that rule handled separately (see below).
- **Hand-fixed**:
  - `xUnit1042` (2x, `CrossProcessorParityTests.cs`): converted `CaseNames()`/`FormatNames()` from `IEnumerable<object[]>` to `TheoryData<string>` via `[.. collection]` collection-expression syntax.
  - `CA1859` (6x): two test-local `Query(...)` helpers (`ImageCommandParserTests.cs`, `HmacSignerTests.cs`) changed return type `IQueryCollection` → concrete `QueryCollection`; four `IOriginalImageSource source = CreateSource();` locals in `AzureBlobOriginalImageSourceTests.cs` changed to the concrete `AzureBlobOriginalImageSource` (its `CreateSource()` helper already returned the concrete type). Removed the now-unused `Umbraco.Image.Processing.Core.Storage` `using` from that file.
  - `CA1816` (1x): added `GC.SuppressFinalize(this)` to `LocalDiskDerivativeImageCacheTests.Dispose()`.
  - `ASP0027` (2x): removed the explicit `public partial class Program;` marker from `src/Umbraco/Program.cs` and the Service's `Program.cs`. Verified empirically (not just trusting the diagnostic) by rebuilding both `Umbraco.csproj` and the cross-assembly `Umbraco.Tests.csproj`/`Service.Tests.csproj`, which reference `WebApplicationFactory<Program>` from a separate test assembly — the SDK's implicit top-level-statements `Program` class is already public on this SDK (10.0.102), so the marker really was redundant, not just quiet.
  - Follow-on `IDE0090` (3x, not in the original ticket list — only appeared after the `CA1859` return-type fix made `new QueryCollection(...)` redundant, plus one pre-existing occurrence in `src/Umbraco.Tests/ImageProcessingPipelineTests.cs` unrelated to this ticket's own edits): simplified to target-typed `new(...)`.
- **Deliberately left alone — `IDE0130` (3x, `src/Umbraco/E2ETestSupportEndpoints.cs`, `ImageProcessingDemoEndpoints.cs`, `OriginImageEndpoints.cs`)**: these three files declare `namespace ImageProcessingDemo`, which doesn't match the `Umbraco` project/folder. Left as-is: it's self-consistently used (`Program.cs` has a matching `using ImageProcessingDemo;` to call `app.MapImageProcessingDemo()`), and renaming it to `Umbraco` would be actively worse here — this project is a reference/demo Umbraco site that itself references dozens of `Umbraco.Cms.*` namespaces, so a bare `namespace Umbraco` would be needlessly confusable with those. Read as an intentional naming choice for "this project's own demo-endpoint code" rather than an oversight.
- **Directory.Build.props**: added a second `<PropertyGroup>` with `<TreatWarningsAsErrors>true</TreatWarningsAsErrors>` and `<EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>`. After adding it, a full `dotnet build -t:Rebuild` on the solution was still 0 warnings/0 errors with no further fixes needed — `EnforceCodeStyleInBuild` did not escalate the intentionally-left `IDE0130` occurrences to build warnings (its default "suggestion" severity apparently stays below the build-warning threshold), so no `.editorconfig` suppression was needed either.
- **Test results — no regressions, same counts as ticket 07's last-recorded baseline plus the new suites**: `dotnet test` on the full solution, all green: Core.Tests 71, UmbracoExtensions.Tests 10, ImageFlow.Tests 19, SkiaSharp.Tests 17, ParityTests 15, Service.Tests 9, Umbraco.Tests 7, E2E.Tests 3 — plus AzureBlob.Tests 12, run separately with a locally cached `mcr.microsoft.com/azure-storage/azurite` Docker image available (its `AzuriteFixture` self-orchestrates the emulator via Aspire's `DistributedApplicationTestingBuilder`, so no manual container setup was actually needed once Docker was confirmed available). 163 tests total, 0 failed, 0 skipped.
- **Not touched**: the pre-existing uncommitted ticket-06 changes (`06-cross-processor-parity-suite.md`, the `.sln` project-reference addition, `src/Umbraco.Image.Processing.ParityTests/`) were left as-is content-wise except for this ticket's own mechanical sweep over `CrossProcessorParityTests.cs` (`IDE0290`, `IDE0305`, `xUnit1042`), which is expected per the ticket's own constraints.
